const Router = require("express").Router();

// Router.use('/meta-data', require('./meta-data/routes'));
// Router.use('/profile', require('./profile/routes'));
// Router.use('/client-user', require('./client-users/routes'));

module.exports = Router;